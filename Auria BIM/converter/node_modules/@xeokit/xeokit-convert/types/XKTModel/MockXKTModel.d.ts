/**
 * A mock {@link XKTModel} that creates {@link Mesh}es and {@link Geometry}s to visualize the output of {@link parseGLTFIntoXKTModel}.
 *
 * @private
 */
export class MockXKTModel {
    /**
     *
     * @param cfg
     */
    constructor(cfg?: {});
    _handlePrimitive: any;
    _handleEntity: any;
    geometries: {};
    createGeometry(params: any): void;
    createEntity(params: any): void;
    finalize(): void;
}
